from setuptools import setup

setup(
    name="solitaire_player",
    version='0.1',
    description=("A package to play around with machine learning by "
                 "playing solitaire"),
    author='Nolan Brubaker',
    packages=['solitaire_player'],
)
